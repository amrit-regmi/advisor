# Core
flask>=3.0
python-dotenv>=1.0
requests>=2.31
psycopg2-binary>=2.9

# LLM APIs
openai>=1.30          # used for both Groq and OpenRouter (OpenAI-compatible)

# Data
yfinance>=0.2.40
pandas>=2.0
numpy>=1.26

# NLP / Sentiment
transformers>=4.40
torch>=2.0            # CPU build is sufficient; GPU optional

# Portfolio optimisation
pyportfolioopt>=1.5
scipy>=1.11

# Backtesting (optional — only needed to run pipeline/backtester.py)
backtrader>=1.9.78
